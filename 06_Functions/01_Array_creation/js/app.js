function createArray(rows) {
    const arr = [];
    for (let i = 1; i <= rows; i++) {
      arr.push(i);
    }
    console.log(arr);
  }
  let result = createArray(10);
  console.log(result);
