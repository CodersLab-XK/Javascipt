const n = 13;
// Write code under this comment.
  
for(let num = 0; num < n; num ++){
    if(num % 2 === 0 ){
        console.log( num + " - " + " even");
    } else {
        console.log(num + " - " + "odd");
    }
  }